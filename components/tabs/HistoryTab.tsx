import type { CountryData } from '@/lib/types';

type Props = { data: CountryData };

export default function HistoryTab(_props: Props) {
  void _props;
  return (
    <div className="text-sm text-slate-500">Coming soon.</div>
  );
}
